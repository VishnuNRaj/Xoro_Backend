import * as Responses from '../responses/Interfaces/ChatResponseInterface';
import * as Repository from '../repository/Post/UserPostRepository';
import * as UserFunctions from '../functions/UserFunctions';
import * as ChatEntity from '../../controllers/interfaces/ChatInterface';
import { uploadFileToFirebase } from '../../config/firebase';


export const getChats: Function  = ({user}:ChatEntity.getChats) => {
    try {
        
    } catch (e) {
        return 
    }
}